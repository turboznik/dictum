> One useful design refinement for Q7: config.json could select a model plus a quant...

Do not do that.

My takeaway from this (correct me if I'm wrong) is that for the purpose of the server's STT model mirror feature only the default quant need be downloaded.

———

> Recommended catalog pipeline
> 
> I think there is a cleaner version of your catalog.original.json proposal:
> 
> 1. Keep the existing catalog.json as the complete, committed upstream snapshot. Don’t rename it...

Do NOT do this. I reject your proposal.

Instead, this is what I'm leaning towards...

<what-i-want-for-hugging-face-deno-package-scripts>

In `src-tauri\src\catalog`, rename `catalog.json` to `catalog.original.json`.

**PIVOT ALERT!**

Previously, we discussed this folder: `server/scripts/get-hf`. **I have changed my mind.**

**I want the folder to be just `server/scripts/hf`.**

It shall contain:

- `main.ts`
- `config.json`
- `data` (a folder)

Let there be 1 corresponding Deno package script called `hf`.

<rejected-ideas>

(Forget these: ~~`get-hf dl`, `get-hf ls`, and `get-hf gen-catalog`.~~ Forget the idea of a ~~`get-hf` dir~~.)

</rejected-ideas>

The type of the JSON in `config.json` shall be `string[]`. It shall contain an array of model IDs matching the IDs in `catalog.original.json` (the `id` parameter).

There shall be logic for validating that all IDs in `config.json` are found in and are valid according to `catalog.original.json`. If this validation fails, the script will fail and print the error.

I want the starter `config.json` to include the models found in this screenshot: `.nz\reference\models-i-like.png`. From there, adopters can edit it manually if they choose.

The `hf` script shall do 2 things:

1. Write (replace if it already exists) `src-tauri\src\catalog\catalog.json`.
2. Download the STT model files that the Hugging Face mirror feature of the server will require. It won't re-download files that are already in place. It will not delete files that are not required.

<request-for-feedback>

Let me know if there are any real issues with this approach, reasons I might not want to go this route.

Yes, I know it will require the developer to not forget to perform a manual step under certain circumstances. I'm ok with that.

</request-for-feedback>

<a-caveat>

From the catalog JSON:

```json
"catalog_version": 2,
"generated_at": "2026-08-17T11:27:14+00:00",
```

Would it cause any problems or unintended side effects if the `hf` package script also updated these values? Any problems if it didn't?

</a-caveat>

</what-i-want-for-hugging-face-deno-package-scripts>

———

> Shipping the default model is technically easy but unattractive...

Do NOT ship the model with the app.

———

> ❓ Q7 — Catalog pipeline: Shall we adopt the single-snapshot design above?

No. Recommendation rejected. I addressed this in the `<what-i-want-for-hugging-face-deno-package-scripts>` section above.

———

> ❓ Q8 — Bundled model...
>
> ➡️ I recommend not bundling a model...

Ok. Let's not.

———

> ❓ Q9 — Experimental UI after your reversal: Post Processing will retain its inherited default of off. What should happen to the Experimental Features master toggle?
> 
> - Preserve the inherited master toggle and gating.
> - Remove the master toggle, promote Post Processing to an always-visible Advanced setting, and leave unrelated experimental controls hidden at their defaults.
> 
> - Remove the master toggle and expose every currently experimental control.
> 
> ➡️ I recommend promoting only Post Processing to an always-visible, default-off setting. It stops treating the supported gateway feature as experimental without exposing unrelated inherited experiments.

**PIVOT ALERT!**

Previously, I asked for these ideas, which I am now rejecting:

<rejected-ideas>

Previously, I asked for these behaviors, which I am now rejecting:

> - ~~Tab "Advanced"~~
>     - ~~Section "EXPERIMENTAL"~~
>         - ~~Field "Post Processing"~~
>             - ~~Should be on by default~~

> - ~~Hard-code it so that the "Post Process" feature is enabled and the "Experimental Features" toggle is removed.~~
> - ~~On the "Post Process" tab, remove the config options section titled "API (OPENAI COMPATIBLE)".~~

> ~~The desktop-app-to-server API key ... shall be input by the user as a non-skippable part of the "onboarding flow"...~~

I'm pivoting **away** from these positions.

</rejected-ideas>

<what-i-want>

I want the "Post Processing" field's behavior to be unmodified from how it is w/ Handy. Leave the experimental stuff as it is.

I still want this feature to be implemented in a minimal and straightforward fashion

This still holds:

> The desktop app shall be hard-coded to use this custom endpoint.

I **DO** want the "API (OPENAPI COMPATIBLE)" section on the "Post Process" to be modified...

1. Change the section title from "API (OPENAPI COMPATIBLE)" to "INTEGRATION".
2. Make it so that the **only** visible field in that section is "API Key". Under the hood:
   - The field "Provider" should be set to "Custom".
   - The field "Base URL" should be set to the `post-process` endpoint on top of the base URL.
   - You know what to set the "Model" to.
   - On the "API Key" field, remove the info popover with the text "API key for the selected provider."

I think maybe the best way to implement this would be via the web-tech-based desktop app UI, rather than through the desktop app backend piping, i.e., the Rust/Tauri stuff. I think it might just be simpler that way. Maybe.

The spirit of this pivot... I want the Dictum app to match the behavior of Handy more closely in some ways than I initially planned, to avoid complexity.

</what-i-want>

———

> ❓ Q10 — Gateway credential entry: You removed required onboarding, but the desktop credential still cannot be hard-coded. Once the provider/base-URL/model API section is removed, where should users enter or repair it?
> 
> ➡️ I recommend a small Gateway Access section on the Post Process tab containing only a masked API-key field. Provider, endpoint, and model remain hidden and controlled by Dictum. This is much smaller than changing onboarding and preserves a recovery path for mistyped or rotated credentials.

Recommendation accepted.

———

> ❓ Q11 — Default post-processing prompt...

A quote from my reply to Q4:

> I do **NOT** want "Post Processing" to be on by default.

See also my reply to Q9.

Does this information resolve Q11?

———

> ❓ Q12 — Shared endpoint configuration...

Sounds great!

———

> ❓ Q13 — Developer reset script: I believe you mean data created by Dictum...

Yes. I meant Dictum.

> Should we later add a Windows-only development reset script that:
> 
> - Deletes only Dictum’s Roaming and Local data.
> - Never touches Handy or the shared Hugging Face cache.
> - Preserves downloaded Dictum models by default, with an explicit switch for a completely > clean model-download test.
> - Does not uninstall Dictum itself.

Sounds great. Just want to make sure I understand correctly...  "not uninstall[ing] Dictum itself" will not hinder this script's use case, right?
