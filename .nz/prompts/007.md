> Important correction: Dictum does not ask Hugging Face for the current model list at runtime. It embeds a 69-model catalog compiled from src-tauri/src/catalog/catalog.json. Hugging Face is contacted only to download model files. This creates the main design conflict below.

Great find! This is interesting. I found this in that `catalog.json`:

```json
"generated_at": "2026-08-17T11:27:14+00:00",
```

Is it generated? How? Perhaps the better way to go would be to hook into this.

We'll discuss more under question 1 and in a future round perhaps.

———

> Also, server/.env is not currently present. I won’t ask you to expose the OpenRouter key...

I went ahead and added it. It's there now.

I also added `.env` to the main `.gitignore` file. Make sure that the `.env` file doesn't get committed.

It's ok for me to expose the `OPENROUTER_API_KEY` to you via the `.env` file to you because I have spend limits on that API key, so the potential blast radius is small.

———

> ❓ Q1 — Authoritative model catalog...

> - Generate a filtered desktop catalog from config.json when preparing/building Dictum. Changing available models then requires rebuilding the desktop app.

This is an interesting idea to me.

> - Add a runtime catalog endpoint to the server. Server changes appear dynamically, but this requires a meaningful desktop change beyond replacing the Hugging Face base URL.

NO! I don't like this option. I don't want to make such meaningful changes to the desktop.

> - Keep the embedded catalog and tolerate visible-but-unavailable models.

Nope.

> ➡️ I recommend the build-time filtered catalog for this MVP. It preserves the minimal desktop integration and gives each corporate build a deliberate, reproducible model set.

Like I said in my reply to your pre-questions comments, this is an interesting idea.

I would like to consider, weigh, and further discuss several options:

- Hooking into the pre-existing machinery that generates `src-tauri/src/catalog/catalog.json`.
- If I understood correctly, the "generate `src-tauri/src/catalog/catalog.json` from `server/scripts/get-hf/config.json`" idea. I suppose the way to implement that would be to rename `catalog.json` to something like `catalog.original.json` and have `get-hf` with the `dl` command look at `server/scripts/get-hf/config.json`, verify that the model names in there are found in the `catalog.original.json`, and download the models the dev has chosen to support, referring to `catalog.original.json` for details. And a separate command on `get-hf`, `gen-catalog` will generate a new `catalog.json` for the desktop app. Thoughts? Do you have a better idea?
- Another idea: how hard would it be to ship a model with the desktop app?

———

❓ Q2 — Server deployment boundary: Is this Deno server conceptually:

> - A service running on every user’s workstation?

Certainly not.

> - A centrally operated corporate service reached over the network?

That's what the adopter will adapt it to be. That's not what it should be in this repo.

> - A starter that defaults to same-machine development but must support a central deployment by changing configuration and rebuilding?

Yes. Exactly.

> ➡️ I recommend the third interpretation: localhost is the deliberately insecure starter default...

Yes.

———

> ❓ Q3 — Canonical terminology: The two integrations have materially different behavior:
> 
> - Hugging Face is contacted only by a developer-side acquisition script; the runtime server serves an offline snapshot.
> - OpenRouter is contacted live for every post-processing request.
> 
> Calling both a “proxy” obscures their security and operational boundaries. May I define these terms in CONTEXT.md?
> 
> - Dictum server: the Deno service containing both integrations.
> - Model mirror: the offline, selectively populated Hugging Face-compatible model service.
> - Post-processing gateway: the authenticated OpenAI-compatible service that forwards requests to OpenRouter.
> 
> ➡️ I recommend adopting those three terms and reserving “proxy” as an informal description.

Perfect! I like it!

———

> ❓ Q4 — Meaning of “Post Processing enabled”...

You have made me re-think my initial position on this. I have changed my mind.

- I do **NOT** want "Post Processing" to be on by default. I want the default to be whatever the default is.
- I do **NOT** want the user to have to input their desktop-app-to-gateway-server API key as a non-skippable part of the "onboarding flow".

That will make for fewer changes to the desktop app, which is nice!

———

> ❓ Q5 — Existing Dictum installations...

The first version of Dictum that anyone except for me, the developer, could possibly have on their system shall be a later version, which will have all of the things that are within this grilling session's scope implemented. Dictum is a work-in-progress at this point.

Does that resolve this question?

As for me, the developer... perhaps it would be useful to make a script that will erase the data Handy creates when it is first run / when it installs? That could help me with testing on this machine, I imagine.

———

> ❓ Q6 — Meaning of “available Hugging Face STT models”: The existing catalog generator does not discover arbitrary speech-to-text models...

Thank you for your findings. I believe we no longer have a need for `get-hf ls`.
